# Second Entry (H2/L2) — Python dependencies.
#   pip install -r requirements.txt
#
# MetaTrader5 is Windows-only; the platform marker lets this file install
# cleanly on Mac/Linux too (for editing/backtests) — it just skips MT5 there.
MetaTrader5>=5.0.45 ; platform_system == "Windows"
pandas>=2.0
numpy>=1.24
pyarrow>=12.0        # reading the M1 parquet data for backtests / paper trading
